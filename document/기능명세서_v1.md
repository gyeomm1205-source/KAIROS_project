# 기능 명세서

- v1
    
    
    | No. | 화면분류 | Depth1 | Depth2 | Depth3 | 기능 및 상세 설명 | 구분(우선순위) | 비고(기술) |
    | --- | --- | --- | --- | --- | --- | --- | --- |
    | #1 | 메인 (Home) | 랜딩페이지 | 서비스 소개 |  | **플랫폼 주요 기능(캘린더, 추천, 연동 등) 소개**
    
    페이지 스크롤 시 상단에 고정되는 Sticky Navbar 제공, 각 메뉴 클릭 시 해당 영역으로 이동(smooth scrolling) , 스크롤 위치에 따라 현재 활성화된 영역 표시(scrollspy)  | Must Have | Frontend: `position: sticky`, `scroll-behavior: smooth`, Intersection Observer API 활용 |
    | #2 |  |  | 로그인 CTA | 구글로 로그인하기 | **노출 위치: 헤더 영역 및 메인 콘텐츠 중간 영역**
    (두 위치의 버튼은 동일한 로그인 플로우를 실행함)
    
    “Google로 시작하기” 클릭 시, Google OAuth로 리다이렉트
    
    인증 성공 시, 로그인 처리 후 사용자 상태에 따라 다음 화면으로 이동
      • 최초 로그인(온보딩 미완료): 온보딩 플로우(`#3`)로 이동
      • 기존 로그인(온보딩 완료): 캘린더 페이지(`#13`)로 이동
    
    인증 실패/취소 시, 로그인 전 상태 유지(재시도 가능) | Must Have | Backend: Google OAuth 2.0 API.
    
     |
    | #3 | 온보딩 | 초기 설정 | 외부 계정 연동 |  | **최초 로그인 사용자에게만 노출되는 단계**
    
    사용자는 필수적으로  GitHub + Velog 모두 연동을 진행해야 함
    
    ”GitHub 연동하기”, “Velog 연동하기” 버튼을 눌러서 연동 진행
    
    연동 완료 후 ”다음” 버튼 클릭 시, 사전 설문(`#4`)으로 이동 | Must Have | **[데이터 추출 파이프라인]
    
    0305 변경사항 : keyBert 일단은 안 쓸듯? 기술스택 분류’만’ 할 거면 keybert가 유용한데, 우리는 맥락파악이 필요해서 summary가 어차피 있어야 함 .. llm이 1~2줄로 요약할 때 기술스택분류 + 카테고리분류(일상/학습 등등)까지 같이 한번에 하면 될 듯**
    
    1. **데이터 Fetch:** Velog(글 본문), GitHub(핵심파일- readme /`package.json` /`build.gradle` / `pom.xml/ requirements.txt`) , 구글캘린더 - 날짜, 제목, description 수집
    
    2. **LLM 구조화 (Structured Output):** 수집된 텍스트를 경량 LLM(4o-mini 등)에 통과시켜 JSON 추출 (`tech_stacks`, `summary`, `category(일상/학습/개발/취준)`)   
    
    3. **DB 통합 적재:** `user_activity` 단일 테이블에 `source`(velog/github)와 함께 로깅
    
    4. **점수화:** 산출된 스택으로 `learning_states` 1:N 테이블에 점수 Upsert
    
    user_activity 테이블 구조 
    
    id (PK)
    
    user_id (FK)
    
    source: velog, github, google_calendar, app_quiz, app_curriculum 등
    
    category: 학습, 개발, 취준, 일상
    
    tech_stacks: ["pytorch", "tensorflow"] (JSON/Array 형태)
    
    summary: "간단한 RNN과 LSTM 레이어 연습"
    
    activity_date: 2026-03-05 14:00:00
    
    is_excluded: False (나중에 히스토리에서 삭제하면 True로 변경) |
    | #4 |  |  | 사전 설문 |  | **초기 분석에 필요한 사용자 정보를 수집하는 단계**
    
    입력 항목
      • 연동 정보 범위(학습/개발/취준/일상)
      (0310추가)
    연동 정보 범위 수정
    1. '연동 정보 범위 선택' 에서의 선택 항목 중에 '일상' 빼기
    ⇒ FE 구현  시 고민 사항 : 사용자가 이 항목을 읽었을 때, ‘내 캘린더에서 항목을 가져올 때 개발/취준/학습만 가져온다는건가?’라고 오해할 수도 있게 제목을 ‘연동 범위 선택’이라고 지었음 —> 이 선택지를 둔 이유는 커리큘럼 생성 방향을 정하기 위함이므로 그걸 직관적으로 이해할 수 있는 네이밍을 해야함.
    2. '일상'의 영역을 구글 캘린더에서 가져오는 이유 = 발/공부/취준 외 일정을 고려해서 커리큘럼을 짜주기 위함.
    목적이 다르기에 별도의 블록을 만들어서 '예/아니오'의 버튼을 만들어서 선택하도록 함. ‘커리큘럼 제작 시 개발/공부/취준'외 내용도 고려할까요?'라는 질문과 예시를 '친구 결혼식'처럼 사용자가 어떤 의미인지 이해할 수 있도록 텍스트로 제공. 그리고 이 질문 항목의 제목은 '일정 반영 범위' 로 일단 우선적으로 지었음.
      (0306추가) • 일상 관련 내용 마스킹 여부
      : 이 항목에 동의하시면, 민감정보(개인정보)를 포함한 개인적인 일정들은 캘린더에서 ‘기타 일정’으로만 표시됩니다. 
    
      • 현재 직업(라디오 선택)
      • 기술 스택(태그/칩 형태 입력)
      • 희망 포지션(백엔드, 프론트엔드, AI, 디자인, PM, 데이터분석가 등등, 복수선택/잘 모르겠다 선택 가능)
      • 희망하는 에이전트 페르소나(선택 드롭다운) 에이전트 페르소나는 기본값이 “기본”으로 미리 선택되어 있음 → 옛날에 유저와 에이전트 직접채팅이 있던 버전에서 필요했던 기능인데, 지금 버전엔 필요없을듯? 
    
    사용자가 현재 직업 + 기술 스택을 입력하면 “다음”버튼이 활성화됨
    
    ”다음” 선택 시, 분석 로딩 화면(`#5`)으로 이동
    
    **정책**
    이 화면에서는 “건너뛰기” 버튼을 제공하지 않음 ($\because$ 외부 계정 연동과 초기 설문 둘 다 스킵 시 분석 불가) | Must Have | **[데이터 활용 분류]
    직업/희망포지션/페르소나:** RDB 저장 + LLM 호출 시 System Prompt에 Context로 상시 주입 (말투 및 추천 난이도 조절용)
    
    **기술 스택:** `learning_states` DB에 초기 점수(예: 0.5) 할당 + RAG 검색 시 Vector DB의 Metadata Filter(`skill=...`) 조건으로 활용
    
    **연동 정보 범위:** 수집 필터링이 아닌, LLM 주입 전 RDB 조회 시점의 `WHERE category IN (...)` 조건으로 사용
    
    일상 마스킹 여부 : user db를 보고 이 항목이 True 라면, 프론트에서 우리 캘린더에 항목을 기입할 때 이 user_activity 의 category == “일상” 이라면 반드시 “기타 일정” 으로만 표기. 상세내용은 표기하지 않되, 일정생성을 할 때는 이 날짜를 고려하도록(피해가도록) |
    | #5 |  | 데이터 분석 | 분석 로딩 화면 |  | **사용자가 분석을 대기하면서 진행 상태를 확인하는 화면** | Must Have | Frontend: SSE(Server-Sent Events) 또는 Webhook을 통해 백그라운드 분석 진행도를 %로 수신받아 UI 렌더링 |
    | #6 |  |  | 분석 결과 화면 |  | **분석 완료 시 자동으로 진입하는 결과 확인 화면**
    
    최근 활동 기술, 전반적인 스킬 레벨(기술 숙련도), 반복 사용 기술 , (사전설문에서 희망 포지션 잘 모르겠다를 골랐다면) 추천 포지션 등 결과 제공
    
    분석 결과 일치 여부(네/아니오) 수집
    
    네 → 최초 추천`(#8`)으로 이동
    아니오 → 일치하지 않는 부분은 사용자 직접 입력(`#7`)
    
    (0305추가) 이 스킬 레벨은 RDB의 user 테이블의 learning_states 필드에서 관리하게 됨,
    
    사용자의 연동 데이터에 가중치를 부여함 
    
    (예 : A기술 스택 관련 블로그 글 작성은 1회 당 0.2 , 깃 활동은 커밋 당 0.05 , 그런데 오늘 날짜 기준으로 1개월 전 작업이라면 *0.9 , 2개월 전 작업이라면 *0.8 ..  시간에 대한 decay weight 도 주고 싶음. 회원가입 survey에서 본인의 기술 스택에 입력했다면 decay없이 고정 +0.5) 이 점수가 user 테이블에서 계속 관리되고 이걸로 스킬레벨 추정하기, 
    
    근데 이 점수는 시간 가중치가 있으니까 시간이 지나면 또 점수 계산을 갱신해야 함. 1개월 단위로 작업하도록 하면 될 듯? 
    
    user_activity 테이블이 있다고 하면, 이 테이블이 어떤 user id의 것인지 연결되어야 할 거고 (foreign key 맞나?) 작업의 종류(git push인지, 블로그 글인지, 우리 서비스에서 제공한 커리큘럼인지, 퀴즈 활동인지 등등 ) / 작업이 일어난 년월 (2025.08)  / 현재 시간과의 차이( = 몇 개월 전 작업인지) / 작업 종류 별 가중치
    
    이렇게 있으면 바로 계산 가능할 듯! 
    ∑ (작업 종류 상수) * (시간 decay 가중치) + survey 기술 스택 응답 고정 값 
     | Must Have | 이 결과물은 사용자의 learning state를 관리하면서 계속 업데이트할 것이고,
     
    사용자는 앞으로 history 페이지에서 본인의 성장 일지 같은 느낌으로 확인하게 될 것
    
    (추가)
    **[Learning State DB 구조 & Score Algorithm]DB 구조:** 
    
    `learning_states` 1:N 테이블 
    
    (`user_id`, `tech_stack`, `score`, `updated_at`)
    
    **점수 공식:** ∑ (작업 종류 상수) * (시간 decay 가중치) + survey 기술스택 응답 고정값 
    
    **점수 갱신:** `user_activity` 데이터를 스택별로 Group by 하여 점수 계산. 월 단위 Batch Job으로 Time Decay 반영
    
    고민 필요한 부분 : 
    
    skill level을 상 중 하 이런식으로 결정할 때,
    
      1. 고정된 값을 threshold로 쓸 건지?
    (2.5이상은 상, 1.5~2.5는 중, 1.5미만은 하 이런식으로)
    
    2. 모든 이용자들의 점수 분포에 대해서 상대적으로 레벨을 부여할 건지? (react 점수가 존재하는 이용자들 중 상위 25% 에게는 상 , 25~75%에게는 중, 75% 미만에게는 하 부여)
    
    3. 한 이용자의 여러 기술 스택들에 대해 상대적으로 레벨을 부여할 건지?
    ( pytorch 1.0점, R 2.2점, Vue 0.5점이라면 통계 상, 딥러닝 중, 뷰는 하) 이런식으로 .. 
    
    2번 - 시연때 통계로 보여주기좋을듯? React 사용자들 중 당신의 점수가 상위 몇%에요! 대단합니다! 
    3번 - 자기가 부족한 점을 관리한다는 취지에서 프로젝트 취지와 가장 잘 맞음 
    1번 - 원하는 결과 나오도록 통제하기 쉬움 (커트라인 임의로 조정하면 되니까) |
    | #7 |  |  | 분석 피드백 |  | 분석 결과 화면(`#6`)의 잘못된 부분은 사용자 직접 입력으로 수정 후 재분석하면 반영해서 재분석 후 (`#7`)로 이동
    
    돌아가기 누르면 분석 결과 화면(`#6`)으로 이동
     | Must Have | 사용자의 수정 입력값으로 RDB의 `learning_states` 점수를 수동 Override (가장 높은 가중치)
    
    결국 다음 단계 #8로의 이동은
    #6에서 결과가 맞나요 - ‘네’ 응답했을 때만 이동
    
    백엔드에서 최초추천작업을 하는 시점도 ‘네’ 눌렀을 때 실행 (TBD - 시간 얼마나 걸리는지 보고, 로딩화면 추가할 지 결정) |
    | #8 |  | 최초 추천 | 추천 모달 & 피드백 |  | 분석 결과 기반 최초 맞춤형 커리큘럼(학습 내용, 근거 등) 제안
    제안 수락 시 캘린더 자동 업로드(`#11`로 이동) / 거절 시 타 활동 제안 (`#9`로 이동) | Must Have | 최초 추천 로직 
    
    **[Context-Aware Generation]**
    
    1. **Context 조회:** `learning_states`의 스킬 점수(레벨 파악) + `user_activity`의 최근 `summary` 로그 목록(맥락 파악)을 DB에서 조회
    
    2. **LLM Prompting:** 조회한 데이터를 System Prompt에 주입하여 "유저의 최근 고민/학습 맥락이 이어지는 5일치 커리큘럼"을 JSON 형태로 생성 요청
    
    3. 커리큘럼 생성 시 추천의 '근거(Reason)'도 함께 출력하도록 강제
    
    시간 얼마나 걸리는지 보고 오래걸리면 이 작업 돌아가는 동안 사용자가 보고 있을 다른 페이지 구상해야함!
    
    작업중이라는 문구는 `#6` 화면에서 보여주도록 하고 백엔드에서 작업완료됐다는 신호를 보내면 그때 프론트에서 모달 열고 결과물 출력하기. (모달 먼저 열고 로딩시켜서 사용자를 모달 열린 상태로 기다리게 하지 말 것!) |
    | #9-1 |  |  | 다른 작업 추천  | 퀴즈 | 블로그,깃헙 / 캘린더 일정 기반 퀴즈 제안 | Must Have | **DB 적재:** 퀴즈 진행 결과도 `user_activity`에 `source='app_quiz'`, `tech_stacks` 배열과 함께 통합 저장 |
    | #9-2 |  |  |  | 레퍼런스 | 사용자 활동기록을 바탕으로 다음 블로그 작성을 위한 레퍼런스 문서 추천
    
    추천의 근거로 사용자 활동 기록 바탕으로 추출한 관심 분야 함께 표시
    
    레퍼런스 추천 시 URL이 안전한지 + URL 안의 내용이 맞는지, 너무 outdated된 게 아닌지 검증 작업 거치고
    
    검증작업결과도 요약해서 제공
    
    (url : [www.asdf.blog.com](http://www.asdf.blog.com/),
    분류 : 기술블로그,
    안전한 url입니다! ,
    추천이유 : 한국어 형태소분석기 관련 이론과 예시코드가 잘 정리된 블로그,
    *2023년 글이기 때문에 라이브러리 버전 주의 필요) | Must Have | **[Self-Querying RAG 도입]**
    
    1. **Context 조회 (Query 생성):** `learning_states`(스킬 레벨) + `user_activity`(최근 로그)를 조합하여 Qdrant(Vector DB)에 날릴 **검색어와 메타데이터 필터(예: `skill=python`)를 스스로 생성**
    
    (LangChain의 `SelfQueryRetriever`가 있다고 함)
    
    2. **Vector DB Retrieval:** Metadata(스킬, 직무) 필터링 후 검색
    
    3. **검증 및 요약 (LLM):** Vector DB에서 가져온 텍스트 청크(Top-K)를 LLM에 주입. 최신성(Outdated) 판별 및 추천 사유(Reason) 요약
    
    4. **DB 적재:** 최종 추천 내역을 `user_activity`에 `source='app_reference'`로 통합 저장tw
    
    URL의 안정성은 최초 vector DB 만들 때 크롤링하면서 검사
    
    *(※ 보안 검사 및 실시간 크롤링은 실시간 Web Search Fallback 시에만 예외적으로 사용)*  |
    | #10 |  |  | 활동 진행 화면(퀴즈/레퍼런스) |  | `#9`에서 선택한 활동을 생성해서 진행
    활동 완료 후 캘린더 업로드(`#11`로 이동) |  |  |
    | #11 |  |  | 캘린더 업로드 확인 화면(모달) |  | 최초 활동 완료 시 **스포트라이트** 효과와 함께 완료 안내 **코치 마크** 노출. 확인 시 모달이 좌측 캘린더 탭으로 빨려 들어가는 **Fly-to 애니메이션** 적용 후, 캘린더 탭에 **Pulse(반짝임) 애니메이션**을 3회 발생시켜 사용자의 클릭 및 메뉴 인지 유도 (인터랙티브 온보딩) |  | Frontend: CSS Keyframes, Framer Motion 등을 활용한 좌표 이동 애니메이션 구현 |
    | #12 | 캘린더 | 캘린더 뷰 | 월간/주간 일정 |  | Git Branch 형태의 노드와 선을 활용한 시각적 달력 UI
     | Must Have | DB: `curriculums` 및 `schedules` RDB 테이블. 부모-자식 노드 관계를 갖는 Tree 구조 데이터 모델링 필요 |
    | #13 |  |  | 상세 일정 확인 | 날짜 클릭 | 클릭한 일자의 학습 내용 모달 제공 및 상태(완료율 등) 수정/확인 | Must Have | 해당 일자 일정의 description (모달)
    
    `is_completed` 상태값 업데이트 API |
    | #14 |  |  | 사고 과정 확인 | 연결선/노드 클릭 | 해당 커리큘럼이 추천된 에이전트의 사고 과정 대시보드 모달 제공 | Must Have | 해당 커리큘럼의 사고 과정(모달)
    
    추천(8번) 생성 시, LLM의 CoT(Chain-of-Thought) 추론 로그나 생성 근거(Reference URL)를 RDB에 JSON 형태로 저장해 두었다가 호출
     |
    | #15 |  |  | 외부 캘린더 연동 |  | 마지막 동기화 일시 /  수동 동기화 버튼 (불러오기) 출력
    
    (0306추가) 항목을 가져올 때 (개발, 학습, 취준, 일상) 에 따라 가벼운 llm에게 카테고리 분류하게 하고, `#4`에서 사용자가 민감 정보를 마스킹하도록 요구했다면 “일상” 카테고리는 “기타 일정”으로만 표기하기
    
    사용자가 구글캘린더에서 일정(커리큘럼) 이름이나 기간 등등을 변경해서 우리 캘린더와 달라져서 추적이 끊어지는 상황을 위해 Google Calendar Api에 extendedProperties 라는 게 있음 | Must Have | **Google Calendar API**
    
    일정 텍스트를 LLM(4o-mini)에 넘겨 카테고리/요약/기술스택 추출 후
    `user_activity`에 `source='google_calendar'`로 통합 저장
    
    `extendedProperties`에 내부 curriculum_id 매핑
    
      • 예외처리나 상세 로직 부분 진우님이 구체화 중
    
    프론트 : user의 want_masking 값이 True라면 user_activity 항목의 category가 ‘일상’ 일 경우 ‘기타 일정’으로만 표기하기 |
    | #16 | 추천 | 기존 학습 진행 | 맞춤 추천 노출 | 학습 추천 / 레퍼런스 추천  | **이전 학습 맥락을 이어가는 맞춤형 추천 (3단 레이아웃 UI)
    
    상단:** 이전 학습에 이은 다음 단계 제안 근거 (사고과정)
    
    **좌측:** 복습 퀴즈가 포함된 '행동 단위 학습 미션'
      1. 이전 학습 맥락의 복습 퀴즈 제안(예상 시간, 바로가기 → 모달로
      2. velog (github TIL)에 레퍼런스 참고해서 글 작성하기 (오른쪽에 레퍼런스 제공)
      3. 커리큘럼 추천 받기
    
    **우측:** 심화 학습을 위한 맞춤형 RAG 레퍼런스
    
    +제안을 바탕으로 캘린더에 커리큘럼을 추가해 볼까요? 버튼이 여기 있으면 될 듯 
    
    레퍼런스 추천 시 URL이 안전한지 + URL안의 내용이 맞는지, 너무 outdated된 게 아닌지 검증 작업 거치고
    
    검증작업결과도 요약해서 제공 
    
    (url : www.asdf.blog.com, 
    분류 : 기술 블로그, 
    안전한 url입니다! , 
    추천 이유 : 한국어 형태소 분석기 관련 이론과 예시 코드가 잘 정리된 블로그, 
    *2023년 글이기 때문에 라이브러리 버전 주의 필요) | Must Have | `user_activity` 테이블에서 해당 유저의 최근 로그를 조회하여 LLM Context로 주입 후 연관 RAG 검색
    
    #8의 [통합 추천 파이프라인]과 동일한 로직 구조 사용 |
    | #17 |  |  | 성취도 반영 | 복습 퀴즈 제안 | 학습 후/일정 간격 후 퀴즈 결과에 따라 기존 커리큘럼 유지/조정/심화 제안 | Must Have | Learning State 실시간 업데이트
    퀴즈 정답 시 `learning_states` 테이블의 해당 스킬 점수 +0.1, 오답 시 +0.05 반영(Upsert) |
    | #18 |  | 신규 학습 시작 | 신규 주제 입력 | 사전 퀴즈 제안 | 새로운 공부 시도 시 목표 입력 및 사전 퀴즈(기초 수준 파악) 진행 | Must Have | Zero-shot 기반 퀴즈 생성. 결과에 따라 `learning_states` 테이블에 신규 스택 초기 점수 Insert |
    | #19 |  |  | 커리큘럼 생성 |  | 사전 퀴즈 결과를 기반으로 사고과정(상단) + 액션 미션(좌측) + 기초 레퍼런스(우측) 제공 및 캘린더 추가
    `#16` 화면을 같이 쓸 듯  | Must Have | **[Context-Aware Generation]**
    
    커리큘럼 생성 LLM 프롬프트에 `user_activity` 테이블의 `summary` 로그 목록을 시간순으로 주입하여, 과거 맥락이 이어진 상세 일정을 생성하되 사용자의 예정된 일정(예: 다음주 주말에는 MT가 있으므로 다른 일정 못 넣음 등등..) 반영 |
    | #20 | 히스토리 | 히스토리 뷰 | 추천 맥락 관리 |  | 과거 추천 내역, 학습 기록, 퀴즈 진행 결과 리스트업 | Must Have | `user_activity` 단일 테이블을 `activity_date` DESC 기준으로 조회하여 리스트업
    
    쇼핑몰 주문목록마냥
    특정 카테고리, 특정 기간에 대해서도 조회할 수 잇게 |
    | #21 |  |  | 기록 삭제/제외 |  | 원치 않는 특정 활동 기록 삭제 시, 이후 추천 알고리즘 컨텍스트에서 제외 | Must Have | **Soft Delete 적용** `user_activity` 레코드 완전 삭제가 아닌 `is_excluded = True` 업데이트. (제외한 맥락은 사용자 화면상에서도 아예 지워버리는것보다, 회색으로 비활성화처리해두고 원하면 다시 원복할 수 있게? 하는게 나을듯! 어차피 is_excluded만 다시 False로 바꾸면 되는거니 )
    
    이후 LLM Context 조회 시 `WHERE is_excluded = False` 조건 적용 |
    | #22 |  | 나의 성장 일지(가제) | 나의 데이터 요약 확인 |  | `#6`에서 가입 직후 보여준 정보 요약을
    사용자 활동이 추가됨에 따라 상시 업데이트해줌
    
    `#6`의 정보요약을 완전히 그대로 쓰는 것이 아님
    가입직후 정보요약은 ‘당신이 현재 어떤 상태인가’
    `#22`에서 보여줄 정보들은 ‘당신이 우리서비스를 이용하면서 어떤 혜택들을 받았나, 얼마나 성장했나’ 중점으로 시각화와 통계 제공 | Must Have | **`#6`**에서 최초 생성한 learning state 모델의 기술별 점수를 우리 서비스 이용할 때마다 계속 관리하기
    
    `user_activity` 빈도 수 및 `learning_states`의 스킬별 점수를 바탕으로 성장 그래프(스파이더 차트 등) 렌더링
    
    프론트 - 요거 보여줄 화면 하나 더 필요함! `#6`의 최초요약분석과 비슷하지만 포인트가 좀 다름. |
    | #23 | 마이페이지 | 프로필 설정 | 기본 정보 관리 |  | 닉네임, 입력한 기술 스택, 관리할 정보 범위(학습/개발/취준/일상) , ~~에이전트 페르소나(삭제)~~ 개인일정 마스킹 여부(`#4`에서 추가) 확인 및 수정 기능 | Must Have | API: `PATCH /users/profile`. 
    
    정보 범위(학습/일상 등) 변경 시 이후 `user_activity` 조회 조건이 실시간 변경됨 |
    | #24 |  | 서비스 연동 | 외부 계정 관리 |  | 연동된 GitHub, Velog 상태 확인 및 ~~추가 연동 버튼~~
    마지막 연동 일시 확인, 수동 동기화 버튼 | Must Have | OAuth Access Token 갱신 및 만료 관리 로직 (Refresh Token) 민재님이 잘 하시겠지~ |
    | #25 |  |  | 추가 계정 연동 |  | Tistory, Naver 블로그 OAuth 연동 지원 | Could Have | 각 플랫폼별 OAuth 규격 및 Open API(글쓰기 API 등) 연동 (우선순위 매우 낮음) |
    | #26 |  | GitHub 심층분석 | 코드/리드미 리딩 | 마이페이지 내 | 에이전트가 GitHub 레포지토리와 리드미를 리딩하여 실제 기술 스택 구조화
    
    velog 데이터로 우선 학습기록, 관심사, 수준파악 로직과 추천이 잘 작동하면
    
    똑같은 로직으로 데이터만 github데이터로 바꿔서 기능 고도화하기 | Could Have | AST(Abstract Syntax Tree, 예: tree-sitter) 분석 도구 도입 검토 (백엔드 확인요망, 추가개발영역)
    
    MVP의 Dependency 파일 분석 수준을 넘어, 파일 단위의 실제 코드 구현 패턴 및 심화 숙련도 추출 (안 할 가능성 매우매우 높음)
    
     |
    | #27 |  |  | 포트폴리오 추천 |  | 리딩 결과를 바탕으로 포트폴리오에 포함하면 좋을 내용 자동 추천 | Could Have | LLM Prompting: RDB에 적재된 분석 결과를 이력서/포트폴리오 양식(STAR 기법 등) 프롬프트에 주입하여 텍스트 생성   우선순위낮음
     |
    | #28 | 콘텐츠 생성 | 블로그/노션 작성 | 초안 자동 생성 | 학습 직후 | 사용자가 학습한 내용을 바탕으로 Tistory/Naver/Notion 업로드용 초안 생성 | Could Have | Markdown 포맷 컨버터 적용. LLM에게 사용자 페르소나(블로그 글쓰기 스타일)를 System Prompt로 주입
    우선순위낮음 |
    | #29 | 공통/예외 | 예외 처리 | 세션 종료 안내 |  | 설문 도중 이탈 시 다음 로그인에 이어서 진행할 수 있도록 재안내 | Changed | 3.5 회의에서 바뀐 부분 - 
    
    사용자 role을
    
    최초 구글로그인&가입시 db에 해당user 생성 직후 guest
    
    모든 설문단계를 완료시 surveyed
    
    결과요약을 확인하고 최초추천까지 받고 첫 작업까지 완료했으면 user
    
    세가지로 분류하고
    
    그외에 관리자(admin) 
    
    만약 다시 로그인을 했는데 role이 guest라면 ? 무조건 survey 처음 단계부터 진행
    
    role이 surveyed 라면 ? 분석결과요약 페이지로 보내도록
    
    다만 프론트에서, 온보딩단계 진행 중 페이지를 이탈하려고 하면 *중단 시 처음부터 다시 진행하셔야 합니다. 정말 종료하시겠습니까? 정도 alert만 해주면 될 듯 |
    | #30 |  |  | 미연동 알림 |  | GitHub/Velog 미연동 상태 진입 시 맞춤형 기능 제한 안내 및 연동 유도 | Changed | 서비스연동은 진행과정에서 필수로 바뀌었음 |
    | #31 |  |  | 미응답 처리 |  | 퀴즈 등 미응답 상태 누적 시 일정 리마인드 또는 추천 강도 자동 완화 | Changed | 퀴즈를 추천받았으나 미진행했다면 해당 기술스택의 learning state 수치 감소하도록 (사용자의 action을 추천강도에 반영) |
