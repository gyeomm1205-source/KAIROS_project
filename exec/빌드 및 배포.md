# 빌드 및 배포 정보
- 본 문서는 Gitlab 소스 클론 이후 빌드 및 배포를 위해 정리한 문서입니다.
- 시스템 각 요소를 빌드하기 위해 필요한 정보를 요소별로 정리하였습니다.

<br>

## Vue.js
- **버전 정보**
  - IDE: VSCode
  - version: 24.12.0
- **의존성 (package.json)**
  ```
  {
    "name": "kairos",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "dev": "vite",
      "build": "vite build",
      "preview": "vite preview"
    },
    "dependencies": {
      "axios": "^1.6.0",
      "pinia": "^2.1.7",
      "three": "^0.183.2",
      "vue": "^3.4.0",
      "vue-router": "^4.3.0"
    },
    "devDependencies": {
      "@vitejs/plugin-vue": "^5.0.0",
      "vite": "^5.0.0"
    }
  }

  ```
- **빌드 방법**
  - 프로젝트 디렉토리 접속
    ```
    cd vue-fe
    ```
  - npm 설치
    ```
    npm install
    ```
  - dist 폴더 build
    ```
    npm run build
    ```
  - 서버에 dist 폴더 복제
    ```
    scp -i J14A506T.pem -r dist ubuntu@j14a506.p.ssafy.io:~/dockerBuild
    ```
  - 서버에서 nginx 폴더 생성 및 디렉토리 이동
    ```
    mkdir nginx
    cd nginx
    ```
  - default.conf 파일 작성
    ```
    server {
        listen 80;
        server_name j14a506.p.ssafy.io;
        return 301 https://$host$request_uri;
    }
    server {
        listen 443 ssl http2;
        server_name j14a506.p.ssafy.io;

        ssl_certificate /etc/letsencrypt/live/j14a506.p.ssafy.io/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/j14a506.p.ssafy.io/privkey.pem;

        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location /api/v1/ai/ {
            proxy_pass http://fastapi:8000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffering off;
            proxy_read_timeout 300s;
            proxy_connect_timeout 60s;
            proxy_send_timeout 300s;
        }

        location /api/v1/ {
            proxy_pass http://springServer:8080;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffering off;
            proxy_read_timeout 300s;
            proxy_connect_timeout 60s;
            proxy_send_timeout 300s;
        }
    }
    ```
  - nginx 실행
    - docker-compose.yml 파일 통한 빌드 (아래 참고)


## Spring boot
- **버전 정보**
  - IDE: IntelliJ
  - JDK: 21
- **환경 변수**
  - Docker Container에서 실행되므로, Docker Container에 실행되는 버전으로 env 파일을 설정하였습니다.
  ```
  DB_URL=jdbc:mysql://mysql:3306/kairos_db
  DB_USER=root
  DB_PW=<YOUR_DB_PASSWORD>
  REDIS_HOST=redis
  REDIS_PORT=6379
  ACCESS_TOKEN_DURATION=8
  REFRESH_TOKEN_DURATION=168
  JWT_SECRET_KEY=<YOUR_JWT_SECRET>
  GOOGLE_CLIENT_ID=<YOUR_GOOGLE_CLIENT_ID>
  GOOGLE_CLIENT_SECRET=<YOUR_GOOGLE_CLIENT_SECRET>
  GOOGLE_AUTH_URL=https://accounts.google.com/o/oauth2/auth
  GOOGLE_REDIRECT_URL=https://j14a506.p.ssafy.io/google/redirect
  GOOGLE_TOKEN_URL=https://oauth2.googleapis.com/token
  GOOGLE_CLIENT_SCOPE=openid email profile https://www.googleapis.com/auth/calendar
  GOOGLE_USER_INFO_URL=https://openidconnect.googleapis.com/v1/userinfo
  GOOGLE_CALENDAR_API=https://www.googleapis.com/calendar/v3/users/me/calendarList
  GITHUB_CLIENT_ID=<YOUR_GITHUB_CLIENT_ID>
  GITHUB_CLIENT_SECRET=<YOUR_GITHUB_CLIENT_SECRET>
  GITHUB_AUTH_URL=https://github.com/login/oauth/authorize
  GITHUB_REDIRECT_URL=https://j14a506.p.ssafy.io/github/redirect
  GITHUB_CSRF_CODE=<YOUR_CSRF_CODE>
  GITHUB_TOKEN_URL=https://github.com/login/oauth/access_token
  GITHUB_CLIENT_SCOPE=read:user public_repo
  GITHUB_USER_AGENT=Kairos_Github_App
  GITHUB_USER_INFO_URL=https://api.github.com/user
  AI_SERVER_URL=http://fastapi:8000
  AI_COLECT_ASYNC_PATH=/api/v1/ai/github/collect-async
  AI_VELOG_COLLECT_ASYNC_PATH=/api/v1/ai/velog/collect-async
  AI_RECOMMENDATIONS_DAILY_GENERATE_PATH=/api/v1/ai/recommendations/daily-generate
  TEXT_ENCRYPTOR_PASSWORD=<YOUR_ENCRYPTOR_PASSWORD>
  TEXT_ENCRYPTOR_SALT=<YOUR_ENCRYPTOR_SALT>
  ```
- **빌드 방법**
  - 프로젝트 경로로 이동
    ```
    cd springboot-be
    ```
  - jar 파일 빌드
    ```
    ./gradlew build -x test
    ```
  - jar 파일 경로로 이동
    ```
    cd build/libs
    ```
  - dockerfile 작성
    ```
    FROM eclipse-temurin:21-jdk

    WORKDIR /app

    COPY ./springboot-be-0.0.1-SNAPSHOT.jar app.jar

    ENTRYPOINT [ "java", "-jar", "app.jar" ]
    ```
  - docker 이미지 빌드
    ```
    docker build -t kairos-server .
    ```
- **의존성 (build.gradle)**
  ```
  plugins {
      id 'java'
      id 'org.springframework.boot' version '4.0.3'
      id 'io.spring.dependency-management' version '1.1.7'
  }

  group = 'com.ssafy'
  version = '0.0.1-SNAPSHOT'
  description = 'springboot-be'

  java {
      toolchain {
          languageVersion = JavaLanguageVersion.of(21)
      }
  }

  configurations {
      compileOnly {
          extendsFrom annotationProcessor
      }
  }

  repositories {
      mavenCentral()
  }

  dependencies {
      implementation 'org.springframework.boot:spring-boot-starter-actuator'
      implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
      implementation 'org.springframework.boot:spring-boot-starter-json'
      implementation 'org.springframework.boot:spring-boot-starter-security'
      implementation 'org.springframework.boot:spring-boot-starter-validation'
      implementation 'org.springframework.boot:spring-boot-starter-webmvc'
      compileOnly 'org.projectlombok:lombok'
      runtimeOnly 'com.mysql:mysql-connector-j'
      annotationProcessor 'org.projectlombok:lombok'
      testImplementation 'org.springframework.boot:spring-boot-starter-test'
      testImplementation 'org.springframework.boot:spring-boot-starter-webmvc-test'
      testImplementation 'org.springframework.security:spring-security-test'
      testRuntimeOnly 'com.h2database:h2'
      testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
      implementation 'io.jsonwebtoken:jjwt-api:0.13.0'
      runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.13.0'
      runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.13.0'
      implementation 'org.springframework.boot:spring-boot-starter-data-redis:4.0.3'
      implementation 'com.google.apis:google-api-services-calendar:v3-rev20231123-2.0.0'
      implementation 'com.google.auth:google-auth-library-oauth2-http:1.23.0'
  }

  tasks.named('test') {
      useJUnitPlatform()

      def envFile = file('src/main/resources/env.txt')
      if (envFile.exists()) {
          envFile.readLines().each { line ->
              def trimmed = line.trim()
              if (trimmed && !trimmed.startsWith('#') && trimmed.contains('=')) {
                  def idx = trimmed.indexOf('=')
                  def key = trimmed.substring(0, idx).trim()
                  def value = trimmed.substring(idx + 1).trim()
                  environment(key, value)
              }
          }
      }
  }

  ```

<br>

## Fast Api
- **버전 정보**
  - IDE: VSCode
  - version: 3.12
- **환경 변수**
  ```
  QDRANT_HOST=localhost
  QDRANT_PORT=6333
  QDRANT_COLLECTION_NAME=reference_chunks

  OPENAI_API_KEY=S14P22A506-20649484-f08a-4522-a9fe-a26f5b4a9246
  OPENAI_API_BASE=https://gms.ssafy.io/gmsapi/api.openai.com/v1

  SPRING_SERVER_URL=https://kairos-server:8080/api/v1
  ```
- **의존성 (requirements.txt)**
  ```
  fastapi==0.115.8
  uvicorn[standard]==0.34.0
  python-dotenv==1.0.1
  pydantic==2.11.1

  aiohttp==3.11.14
  httpx==0.28.1
  beautifulsoup4==4.13.3
  lxml==5.3.1
  curl_cffi==0.14.0

  qdrant-client[fastembed]==1.16.2

  langchain==0.3.21
  langchain-core==0.3.49
  langchain-openai==0.3.9
  langchain-google-genai==2.1.2

  openai==1.70.0
  ```
- **빌드 방법**
  - 프로젝트 경로로 이동
    ```
    cd fast-api
    ```
  - dockerfile 작성
    ```
    FROM python:3.12-slim

    WORKDIR /app

    ENV PYTHONDONTWRITEBYTECODE=1 \
        PYTHONUNBUFFERED=1

    COPY requirements.txt ./
    RUN pip install --no-cache-dir -r requirements.txt

    COPY app ./app

    EXPOSE 8000

    CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

    ```
  - docker 이미지 빌드
    ```
    docker build -t ssafy-fastapi .
    ```

<br>

## Redis
- **접속 정보**
  - host: redis
  - port: 6379
- **빌드 방법**
  - docker-compose.yml 통한 빌드 (아래 참고)

<br>

## MySQL
- **접속 정보**
  - 접속 주소: jdbc:mysql://mysql:3306/kairos_db
  - Database 이름: kairos_db
  - username: root
  - password: <YOUR_DB_PASSWORD>
- **빌드 방법**
  - docker-compose.yml 통한 빌드 (아래 참고)

## QDrant
- **접속 정보**
  - host: ssafy-qdrant
  - port: 6333
  - collection_name: reference_chunks
- **빌드 방법**
  - docker-compose.yml 통한 빌드 (아래 참고)

<br>

## Docker 설정
- EC2 서버에 Docker Container를 실행하는 방식으로 구현하였습니다.
- Docker Engine에서 Container 실행 절차를 정리하였습니다.
- docker-compose.yml 파일 작성
  ```yaml
  services:
    mysql:
      image: mysql:8.4
      container_name: ssafy-mysql
      restart: unless-stopped
      environment:
        MYSQL_DATABASE: kairos_db
        MYSQL_ROOT_PASSWORD: <YOUR_DB_PASSWORD>
        MYSQL_ROOT_HOST: "%"
      ports:
        - "3307:3306"
      volumes:
        - mysql_data:/var/lib/mysql
      healthcheck:
        test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p<YOUR_DB_PASSWORD>", "--silent"]
        interval: 10s
        timeout: 5s
        retries: 10
        start_period: 20s

    redis:
      image: redis:7-alpine
      container_name: ssafy-redis
      restart: unless-stopped
      ports:
        - "127.0.0.1:6379:6379"
      volumes:
        - redis_data:/data
      healthcheck:
        test: ["CMD", "redis-cli", "ping"]
        interval: 10s
        timeout: 5s
        retries: 10
        start_period: 10s

    qdrant:
      image: qdrant/qdrant:latest
      container_name: ssafy-qdrant
      restart: unless-stopped
      ports:
        - "127.0.0.1:6333:6333"
        - "127.0.0.1:6334:6334"
      volumes:
        - qdrant_data:/qdrant/storage

    fastapi:
      build:
        context: /home/ubuntu/S14P21A506/fastapi-be
        dockerfile: Dockerfile
          # image: kairos-fastapi
      container_name: ssafy-fastapi
      restart: unless-stopped
      ports:
        - "8000:8000"
      environment:
        - QDRANT_HOST=qdrant
        - QDRANT_PORT=6333
        - SPRING_SERVER_URL=http://springServer:8080/api/v1
      env_file:
        - /home/ubuntu/S14P21A506/fastapi-be/.env
      depends_on:
        qdrant:
          condition: service_started

    springServer:
      image: spring_server_image
      ports:
        - 8080:8080
      container_name: kairos-server
      depends_on:
        mysql:
          condition: service_healthy
        redis:
          condition: service_healthy
      environment:
        - DB_URL=jdbc:mysql://mysql:3306/kairos_db
        - DB_USER=root
        - DB_PW=<YOUR_DB_PASSWORD>
        - REDIS_HOST=redis
        - REDIS_PORT=6379
        - ACCESS_TOKEN_DURATION=8
        - REFRESH_TOKEN_DURATION=168
        - JWT_SECRET_KEY=<YOUR_JWT_SECRET>
        - GOOGLE_CLIENT_ID=<YOUR_GOOGLE_CLIENT_ID>
        - GOOGLE_CLIENT_SECRET=<YOUR_GOOGLE_CLIENT_SECRET>
        - GOOGLE_AUTH_URL=https://accounts.google.com/o/oauth2/auth
        - GOOGLE_REDIRECT_URL=https://j14a506.p.ssafy.io/google/redirect
        - GOOGLE_TOKEN_URL=https://oauth2.googleapis.com/token
        - GOOGLE_CLIENT_SCOPE=openid email profile https://www.googleapis.com/auth/calendar
        - GOOGLE_USER_INFO_URL=https://openidconnect.googleapis.com/v1/userinfo
        - GOOGLE_CALENDAR_API=https://www.googleapis.com/calendar/v3/users/me/calendarList
        - GITHUB_CLIENT_ID=<YOUR_GITHUB_CLIENT_ID>
        - GITHUB_CLIENT_SECRET=<YOUR_GITHUB_CLIENT_SECRET>
        - GITHUB_AUTH_URL=https://github.com/login/oauth/authorize
        - GITHUB_REDIRECT_URL=https://j14a506.p.ssafy.io/github/redirect
        - GITHUB_CSRF_CODE=<YOUR_CSRF_CODE>
        - GITHUB_TOKEN_URL=https://github.com/login/oauth/access_token
        - GITHUB_CLIENT_SCOPE=read:user public_repo
        - GITHUB_USER_AGENT=Kairos_Github_App
        - GITHUB_USER_INFO_URL=https://api.github.com/user
        - AI_SERVER_URL=http://fastapi:8000
        - AI_COLECT_ASYNC_PATH=/api/v1/ai/github/collect-async
        - AI_VELOG_COLLECT_ASYNC_PATH=/api/v1/ai/velog/collect-async
        - AI_RECOMMENDATIONS_DAILY_GENERATE_PATH=/api/v1/ai/recommendations/daily-generate
        - TEXT_ENCRYPTOR_PASSWORD=<YOUR_ENCRYPTOR_PASSWORD>
        - TEXT_ENCRYPTOR_SALT=<YOUR_ENCRYPTOR_SALT>
    nginx:
      image: nginx:alpine
      container_name: vue-nginx
      restart: unless-stopped
      ports:
        - "80:80"
        - "443:443"
      volumes:
        - /home/ubuntu/dockerBuild/dist:/usr/share/nginx/html:ro
        - /home/ubuntu/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
        - ./nginx/conf.d:/etc/nginx/conf.d
        - /etc/letsencrypt:/etc/letsencrypt:ro
      depends_on:
        springServer:
          condition: service_started
        fastapi:
          condition: service_started
  volumes:
    mysql_data:
    redis_data:
    qdrant_data:
  ```
- yml 파일 실행
  ```
  docker compose up -d
  ```